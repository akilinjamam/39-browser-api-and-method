// document is not anable without browser. because it is an object of browser. this objects are called browser api. this are referred as a BOM => Browser Object Model

const name = 15;
document.getElementById('name');